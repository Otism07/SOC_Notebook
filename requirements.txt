# SOC Case Logger Requirements
# Python GUI Application for Security Operations Center Case Management

# Core Python libraries (usually included with Python)
# tkinter - GUI framework (built-in with Python)
# json - Data serialization (built-in)
# datetime - Date/time handling (built-in)
# os - Operating system interface (built-in)
# typing - Type hints (built-in with Python 3.5+)

# Required dependencies
requests>=2.25.0  # For API calls to AbuseIPDB
cryptography>=3.0.0  # For secure storage of API credentials

# Optional dependencies for enhanced functionality
# These packages can be installed for additional features

# For advanced data manipulation (optional)
pandas>=1.3.0

# For enhanced logging (optional - replaces basic logging)
loguru>=0.6.0

# For data validation (optional)
jsonschema>=4.0.0

# For cryptographic hashing (optional - for file hash generation)
hashlib  # Built-in module

# For GUI enhancements (optional)
pillow>=8.0.0  # For image handling in GUI

# Development dependencies (optional)
pytest>=6.0.0  # For testing
black>=21.0.0  # For code formatting
flake8>=3.8.0  # For linting

# Note: This application is designed to work with Python's built-in libraries
# The optional dependencies above can enhance functionality but are not required
# for basic operation of the SOC Case Logger.